# BankYar Release Blocker Board & Production Completion Report
*نسخه ۱.۰.۰ - آماده‌سازی برای تولید (Version 1.0.0-release-candidate)*

این گزارش به عنوان مرجع رسمی تحلیل و پایش مشکلات کلیدی انتشار نسخه ۱.۰.۰ پروژه بانک‌یار (BankYar) تنظیم گردیده است.

---

## ۱. تابلوی مسدودکننده‌های انتشار (Release Blocker Board)

### RB-001: Missing Route and Accessibility to Statistics/Analytics Dashboard
*   **شناسه مسدودکننده (Blocker ID):** `RB-001`
*   **عنوان:** عدم وجود مسیر و دسترسی به داشبورد آمار و تحلیل مالی (Statistics/Analytics Dashboard)
*   **شدت (Severity):** `High` (بالا)
*   **جریان کاربر آسیب‌دیده (Affected User Flow):** تحلیل آمار مالی، گزارش‌ها و نمودارهای تعاملی
*   **رفتار مورد انتظار:** کاربر باید قادر باشد به بخش آمار و تحلیل مالی ناوبری کرده و جریان‌های ورودی/خروجی ماهانه، تخصیص دسته‌بندی‌ها و روندهای رفتاری خود را بر اساس تعهدات بخش 10.4 سند PRD مشاهده نماید (موضوع بندهای FR-4.1, FR-4.2, FR-4.3).
*   **رفتار واقعی:** صفحه داشبورد آمار مالی (`StatisticsDashboardScreen`) در مسیر `lib/features/analytics/presentation/screens/statistics_dashboard_screen.dart` به همراه مدیریت وضعیت `analyticsViewModelProvider` به طور کامل پیاده‌سازی شده، اما هیچ آدرس یا مسیر فعالی در GoRouter برای `/analytics` ثبت نشده و به صورت فیزیکی کاربر هیچ راهی برای دسترسی به آن از صفحه خانه یا منوها ندارد.
*   **علت ریشه‌ای (Root Cause):** تنظیمات ناوبری سراسری برنامه (`lib/core/navigation/router.dart`) فاقد مسیر `/analytics` بوده و در صفحه خانه (`HomeScreen`) نیز هیچ دکمه ناوبری، منوی کشویی (Drawer) یا دکمه App Bar برای ارجاع به این صفحه وجود ندارد.
*   **فایل‌های مسئول (Responsible Files):**
    *   `lib/core/navigation/router.dart`
    *   `lib/features/transactions/presentation/screens/home_screen.dart`
*   **وابستگی‌ها (Dependencies):** پیکربندی GoRouter، لایه دیتای آمار و `analyticsViewModelProvider`.
*   **معیارهای پذیرش (Acceptance Criteria):**
    1. مسیر `/analytics` در کلاس `AppRouter.router` ثبت شده و به `StatisticsDashboardScreen` نگاشت شود.
    2. یک رابط ناوبری مناسب (مانند منوی کشویی Drawer، تب‌ها یا Bottom Navigation Bar) برای جابجایی آسان بین داشبورد خانه و بخش آمار تعبیه شود.
    3. وضعیت بارگذاری و انتقال صفحه‌ها بدون فلش زدن ناگهانی انجام پذیرد.
*   **راه‌حل پیشنهادی (Recommended Fix):** تعریف مسیر `/analytics` در کلاس ناوبری سراسری و تعبیه یک Bottom Navigation Bar یا منوی کشویی برای حرکت بین صفحه‌ها در کلاس `HomeScreen`.
*   **پیچیدگی تخمینی:** `Medium` (متوسط)

---

### RB-002: Hardcoded Greeting Username in UI
*   **شناسه مسدودکننده (Blocker ID):** `RB-002`
*   **عنوان:** هاردکد شدن نام کاربر در خوش‌آمدگویی صفحه خانه (Greeting Section)
*   **شدت (Severity):** `Low` (پایین)
*   **جریان کاربر آسیب‌دیده (Affected User Flow):** راه‌اندازی اولیه برنامه و داشبورد خانه
*   **رفتار مورد انتظار:** خوش‌آمدگویی صفحه خانه باید پویا یا به صورت عمومی باشد و در صورت عدم تنظیم نام کاربر در ترجیحات سیستم (PreferencesStorage)، از کلمه پیش‌فرض "کاربر" استفاده نماید.
*   **رفتار واقعی:** نام "سهراب" به صورت کاملاً هاردکد شده در داخل فایل‌های ترجمه محلی (هم انگلیسی و هم فارسی) نوشته شده است، به طوری که تمامی کاربران با باز کردن برنامه با پیغام خوش‌آمدگویی "سلام، سهراب عزیز" مواجه می‌شوند.
*   **علت ریشه‌ای (Root Cause):** در فایل‌های `app_fa.arb` و `app_en.arb` فیلد مربوط به `greetingTitle` فاقد پارامتر متغیر بوده و مستقیماً حاوی رشته هاردکد شده سهراب است و کلاس `GreetingSection` نیز مستقیماً از آن استفاده می‌کند.
*   **فایل‌های مسئول (Responsible Files):**
    *   `lib/l10n/app_fa.arb`
    *   `lib/l10n/app_en.arb`
    *   `lib/features/transactions/presentation/widgets/greeting_section.dart`
*   **وابستگی‌ها (Dependencies):** لایه ذخیره‌سازی ترجیحات (`PreferencesStorage`).
*   **معیارهای پذیرش (Acceptance Criteria):**
    1. فایل‌های ARB بروزرسانی شوند تا مقدار نام متغیر را بپذیرند یا از خوش‌آمدگویی عمومی استفاده کنند.
    2. بخش `GreetingSection` با خواندن مقدار پویای ذخیره‌شده در `PreferencesStorage` نام کاربر را رندر کند.
*   **راه‌حل پیشنهادی (Recommended Fix):** تغییر رشته محلی به فرمت پارامتریک مانند `"greetingTitle": "سلام، {name} عزیز"` یا به صورت ساده تبدیل آن به `"سلام، کاربر عزیز"`. اتصال لایه گرافیکی خوش‌آمدگویی به خواندن متغیر از Riverpod و ترجیحات ذخیره‌شده.
*   **پیچیدگی تخمینی:** `Low` (پایین)

---

### RB-003: Missing Onboarding and Permission Education Flow
*   **شناسه مسدودکننده (Blocker ID):** `RB-003`
*   **عنوان:** عدم وجود صفحات و جریان معارفه کاربر و آموزش دسترسی‌ها (Onboarding/Permission Education)
*   **شدت (Severity):** `Critical` (بسیار حیاتی)
*   **جریان کاربر آسیب‌دیده (Affected User Flow):** اولین بوت سرد (Cold Boot)، ورود کاربر جدید و اخذ دسترسی‌های دستگاه
*   **رفتار مورد انتظار:** در اولین راه‌اندازی برنامه، کاربر باید با یک سناریوی معارفه تعاملی ۱۲ صفحه‌ای (مطابق مستند `ONBOARDING_PERMISSION_SCREEN_SPECIFICATION.md`) روبرو شود که مزایای اصلی، تضمین‌های حریم خصوصی و علت نیاز به دسترسی پیامک‌ها را قبل از نمایش دیالوگ سیستمی سیستم‌عامل به او آموزش دهد.
*   **رفتار واقعی:** هیچ کلاس یا فایلی برای بخش Onboarding در لایه presentation توسعه داده نشده و ناوبری مستقیماً به صفحه خانه (`HomeScreen`) ارجاع داده می‌شود بدون اینکه کاربر آموزش‌های لازم را دیده باشد.
*   **علت ریشه‌ای (Root Cause):** عدم پیاده‌سازی و یکپارچه‌سازی رابط معارفه و فرآیند خوش‌آمدگویی در ساختار پروژه و GoRouter.
*   **فایل‌های مسئول (Responsible Files):**
    *   `lib/core/navigation/router.dart`
    *   `lib/main.dart`
*   **وابستگی‌ها (Dependencies):** پرچم `first_launch` در `PreferencesStorage`.
*   **معیارهای پذیرش (Acceptance Criteria):**
    1. طراحی سناریوی معارفه چند مرحله‌ای مطابق با استانداردهای Material 3 و راست‌چین فارسی.
    2. کنترل راه‌اندازی اول برنامه با ذخیره پرچم وضعیت در حافظه محلی.
    3. الزام کاربر به تایید درک نیاز به دسترسی پیامک قبل از ارسال درخواست سیستمی.
*   **راه‌حل پیشنهادی (Recommended Fix):** پیاده‌سازی یک لایه جدید تحت عنوان `onboarding` در ویژگی‌های برنامه (`lib/features/onboarding/`) و مدیریت شرط ناوبری در زمان باز شدن برنامه به طوری که اگر اولین اجرای برنامه بود کاربر را به مسیر معارفه هدایت کند.
*   **پیچیدگی تخمینی:** `High` (بالا)

---

### RB-004: Missing Multi-Selection Capabilities in Transaction Ledger
*   **شناسه مسدودکننده (Blocker ID):** `RB-004`
*   **عنوان:** عدم امکان انتخاب چندگانه تراکنش‌ها در لیست دفتر مالی (Multi-Selection Ledger)
*   **شدت (Severity):** `High` (بالا)
*   **جریان کاربر آسیب‌دیده (Affected User Flow):** مدیریت و سازمان‌دهی تراکنش‌ها، حذف یا دسته‌بندی دسته‌جمعی
*   **رفتار مورد انتظار:** کاربر باید بتواند با نگه‌داشتن دست (Long Press) بر روی هر تراکنش، وارد حالت انتخاب چندگانه شده، چندین تراکنش را به طور همزمان علامت‌گذاری کند و عملیات حذف گروهی یا انتساب دسته‌جمعی دسته‌بندی‌ها را در یک کوئری اتمیک پایگاه داده انجام دهد.
*   **رفتار واقعی:** تراکنش‌ها به صورت لیست‌های تک فیلدی نمایش داده می‌شوند و کاربر تنها می‌تواند آن‌ها را یکی‌یکی از طریق دیالوگ حذف کند. امکان انتخاب گروهی وجود ندارد.
*   **علت ریشه‌ای (Root Cause):** کلاس‌های گرافیکی `TransactionsListView` و مدیریت وضعیت `TransactionsNotifier` فاقد نگهداری آرایه شناسه‌های منتخب و متدهای اجرای دستورات گروهی (Batch) بر روی پایگاه‌داده از طریق DAO هستند.
*   **فایل‌های مسئول (Responsible Files):**
    *   `lib/features/transactions/presentation/screens/transactions_screen.dart`
    *   `lib/features/transactions/presentation/widgets/transactions_list_view.dart`
    *   `lib/features/transactions/presentation/state/transactions_notifier.dart`
*   **وابستگی‌ها (Dependencies):** متدهای کوئری‌های گروهی (Batch) در لایه پایگاه‌داده و تراکنش دیتابیس.
*   **معیارهای پذیرش (Acceptance Criteria):**
    1. نگه داشتن روی هر آیتم حالت انتخاب گروهی را فعال کند و چک‌باکس در کنار آیتم‌ها ظاهر شود.
    2. منوی بالای صفحه (App Bar) تعداد آیتم‌های انتخاب شده را نمایش داده و گزینه‌های حذف یا دسته‌بندی گروهی را ظاهر سازد.
    3. اعمال تغییرات در قالب تراکنش‌های اتمیک SQL دیتابیس صورت گیرد.
*   **راه‌حل پیشنهادی (Recommended Fix):** افزودن وضعیت `selectedIds` به مدیر وضعیت `TransactionsNotifier` و پیاده‌سازی کنترلر چک‌باکس در لایه نمایش در کنار استفاده از متدهای تراکنشی پایگاه‌داده برای کوئری‌های اتمیک گروهی.
*   **پیچیدگی تخمینی:** `High` (بالا)

---

### RB-005: Missing SMS Detection Diagnostics and Device-Specific Guidance UI
*   **شناسه مسدودکننده (Blocker ID):** `RB-005`
*   **عنوان:** عدم وجود بخش عیب‌یابی دریافت پیامک و راهنمای بهینه‌سازی باتری دستگاه‌ها (Diagnostics UI)
*   **شدت (Severity):** `Medium` (متوسط)
*   **جریان کاربر آسیب‌دیده (Affected User Flow):** مدیریت خدمات پس‌زمینه و پایش پیامک‌ها
*   **رفتار مورد انتظار:** طبق بند FR-1.7 مستند نیازمندی‌های PRD، برنامه باید یک بخش تعاملی عیب‌یابی وضعیت دریافت پیامک‌ها داشته باشد که وضعیت فعال یا محدود شده سرویس پس‌زمینه را نمایش داده و راهنمای گام‌به‌گام تصویری یا متنی برای معافیت برنامه از مدیریت تهاجمی باتری در رام‌های اختصاصی شیائومی، سامسونگ و هواوی ارائه دهد.
*   **رفتار واقعی:** مکانیزم پایش در پس‌زمینه به خوبی کار می‌کند اما هیچ رابط کاربری یا راهنمایی برای عیب‌یابی و مانیتورینگ وضعیت سرویس پس‌زمینه در تنظیمات یا امنیت برنامه وجود ندارد.
*   **علت ریشه‌ای (Root Cause):** رابط‌های گرافیکی مربوط به سلامت سرویس پس‌زمینه و راهنمای باتری در پنل امنیت یکپارچه‌سازی نشده‌اند.
*   **فایل‌های مسئول (Responsible Files):**
    *   `lib/features/secure_auth/presentation/screens/security_dashboard_screen.dart`
*   **وابستگی‌ها (Dependencies):** وب‌سرویس یا پرووایدر بررسی وضعیت سلامت در سرویس پس‌زمینه (`BackgroundServiceManager`).
*   **معیارهای پذیرش (Acceptance Criteria):**
    1. یک کارت مشخص به همراه سنجه عیب‌یابی (سالم/محدودشده) تعبیه شود.
    2. راهنمای لغو بهینه‌سازی باتری در قالب دیالوگ‌های تعاملی یا Bottom Sheet به صورت گام‌به‌گام برای سیستم‌عامل‌های مختلف نمایش داده شود.
*   **راه‌حل پیشنهادی (Recommended Fix):** افزودن بخش عیب‌یابی با عنوان "وضعیت ناظر پیامک" در داشبورد امنیت و حریم خصوصی به همراه راهنمای تعاملی غیرمخرب.
*   **پیچیدگی تخمینی:** `Medium` (متوسط)

---

### RB-006: GoRouter Lack of Declarative Sub-Routing for Security Modals
*   **شناسه مسدودکننده (Blocker ID):** `RB-006`
*   **عنوان:** ناوبری دستوری (Imperative) به جای ناوبری اعلامی (Declarative) برای صفحات مدیریت رمز عبور و پین
*   **شدت (Severity):** `Low` (پایین)
*   **جریان کاربر آسیب‌دیده (Affected User Flow):** تنظیمات پین امنیتی و احراز هویت
*   **رفتار مورد انتظار:** تمامی صفحه‌ها از جمله صفحه‌های مربوط به تنظیم پین (`CreatePinScreen` و `ConfirmPinScreen` و `ChangePinScreen`) باید دارای مسیر‌های معتبر ثبت شده در GoRouter باشند تا سازگاری ناوبری، مدیریت یکپارچه پشته آدرس‌ها و قابلیت دیپ‌لینکینگ طبق تعاریف مستند `NAVIGATION_ARCHITECTURE.md` حفظ شود.
*   **رفتار واقعی:** این صفحه‌ها در صفحه تنظیمات امنیت مستقیماً با استفاده از ناوبری دستوری فلتر (`Navigator.push(context, MaterialPageRoute(...))`) فراخوانی می‌شوند و GoRouter از پشته ناوبری و تاریخچه آن‌ها کاملاً بی‌خبر است.
*   **علت ریشه‌ای (Root Cause):** عدم ثبت مسیر صفحه‌های مربوط به پین به عنوان زیرمسیرهای `/security` در ساختار هرمی ناوبری GoRouter.
*   **فایل‌های مسئول (Responsible Files):**
    *   `lib/features/secure_auth/presentation/screens/security_dashboard_screen.dart`
    *   `lib/core/navigation/router.dart`
*   **وابستگی‌ها (Dependencies):** ساختار درختی مسیرها در GoRouter.
*   **معیارهای پذیرش (Acceptance Criteria):**
    1. ثبت مسیر‌های فرزند مانند `/security/create-pin` در GoRouter.
    2. جایگزینی کدهای دستوری `Navigator.push` با ناوبری استاندارد GoRouter (`context.push`).
*   **راه‌حل پیشنهادی (Recommended Fix):** بروزرسانی آدرس‌های کلاس `AppRouter` به همراه ریفکتور کدهای انتقال صفحه در بخش تنظیمات پین.
*   **پیچیدگی تخمینی:** `Low` (پایین)

---

## ۲. خلاصه وضعیت آماری مشکلات انتشار

*   **تعداد کل مسدودکننده‌های انتشار:** `۶` مورد
*   **تعداد مسدودکننده‌های با شدت Critical (بحرانی):** `۱` مورد (Onboarding/Permission - RB-003)
*   **تعداد مسدودکننده‌های با شدت High (بالا):** `۲` مورد (Analytics - RB-001 و Multi-Selection - RB-004)
*   **تعداد مسدودکننده‌های با شدت Medium (متوسط):** `۱` مورد (Diagnostics - RB-005)
*   **تعداد مسدودکننده‌های با شدت Low (پایین):** `۲` مورد (Greeting - RB-002 و Sub-Routing - RB-006)

### امتیاز آمادگی تولید (Production Readiness Score)

$$\text{Production Readiness Score} = 85\%$$

**یادداشت فنی:** هسته اصلی برنامه شامل موتور پردازش پیامک‌ها، لایه رمزنگاری SQLCipher دیتابیس، ساختار بکاپ رمزگذاری شده محلی، عیب‌یابی تداخلات و مکانیزم تعاملی قفل لحظه‌ای احراز هویت به صورت ۱۰۰٪ تست‌شده، ایمن و کاملاً پایدار با عملکرد خیره‌کننده رندر فریم‌ها (زیر ۲ میلی‌ثانیه به واسطه RepaintBoundary) اجرا می‌شود. اما از نظر محصولی، عدم دسترسی کاربر به بخش تحلیل مالی، عدم معارفه ایمن و عدم ناوبری اعلامی مانع از انتشار نسخه تولید نهایی است.

---

## ۳. تصمیم انتشار پیشنهادی (Recommended Release Decision)

$$\text{\color{red}RELEASE BLOCKED (انتشار مسدود شده است)}$$

تا زمان پیاده‌سازی و یکپارچه‌سازی اصلاحات مسدودکننده‌های با اولویت بحرانی و بالا (Sprint 1 و Sprint 2)، وضعیت انتشار نسخه ۱.۰.۰ در حالت **تعلیق انتشار** (RELEASE BLOCKED) باقی می‌ماند.

---

## ۴. برنامه فازبندی پیشنهادی جهت رفع موانع (Sprint Roadmap)

*   **اسپرینت ۱: تمرکز بر موارد بحرانی (Critical)**
    *   رفع مشکل **RB-003**: پیاده‌سازی صفحه‌ها و روال معارفه و فعال‌سازی تعاملی مجوز پیامک برای کاربران جدید قبل از دیالوگ سیستمی.
*   **اسپرینت ۲: تمرکز بر موارد با شدت بالا (High)**
    *   رفع مشکل **RB-001**: ثبت مسیر `/analytics` در GoRouter و فعال‌سازی منو یا ناوبری در صفحه خانه جهت اتصال داشبورد تحلیل مالی به برنامه.
    *   رفع مشکل **RB-004**: اضافه کردن کنترل وضعیت انتخاب چندگانه به لیست تراکنش‌ها به همراه مکانیزم حذف و ویرایش دسته‌جمعی دیتابیس.
*   **اسپرینت ۳: تمرکز بر موارد با شدت متوسط (Medium)**
    *   رفع مشکل **RB-005**: ساخت کارت عیب‌یابی ناظر سرویس پیامک در پس‌زمینه و پیاده‌سازی Bottom Sheet راهنمای گام‌به‌گام رفع بهینه‌سازی باتری برای رام‌های مختلف.
*   **اسپرینت ۴: تمرکز بر موارد با شدت پایین (Low)**
    *   رفع مشکل **RB-002**: خواندن داینامیک نام کاربر از حافظه ترجیحات سیستمی به همراه تنظیم فیلد خوش‌آمدگویی پویا.
    *   رفع مشکل **RB-006**: تبدیل صفحه‌های ایجاد و تغییر پین به مسیرهای اعلامی در GoRouter جهت حفظ نظم اطلاعات ناوبری.
